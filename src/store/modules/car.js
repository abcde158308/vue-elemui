/*jshint esversion: 6 */
const state = {
  d:4,
  e:5,
  f:6  
};
const actions = {
    
};

const mutations = {
    
};

const getters = {
    d:state => state.d,
    e:state => state.e,
    f:state => state.f

};
export default{
    state,
    actions,
    mutations,
    getters
};